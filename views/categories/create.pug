extends ../layouts/common
block content
    if errors
        each error in errors
            .alert.alert-danger= error
    //- .container
    //-     .card.add-form
    //-         .card-header Thêm danh mục mới
    //-         .card-body
    //-             form(action="/categories/create", method="POST")
    //-                 div
    //-                     label Tên danh mục
    //-                         input.form-control(type="text", name="name")
    //-                 div
    //-                     label Mã danh mục
    //-                         input.form-control(type="text", name="cate_id")
    //-                 button.btn.btn-info Thêm
    //-         .card-footer 
    form.get_in_touch(action="/categories/create", method="POST")
        h1.title Thêm danh mục
        .container
            .contact-form.row
                .form-field.col-lg-12
                    input#name.input-text(type='text', required='', name="name")
                    label.label(for='name') Tên danh mục
                .form-field.col-lg-12
                    input#email.input-text(type='text', required='',name="cate_id")
                    label.label(for='email') Mã danh mục
                .form-field.col-lg-12(style="text-align: center;")
                    input.submit-btn(type='submit', value='Thêm')