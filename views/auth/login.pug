extends ../layouts/common
block content
    
    
    //- .container
    //-     .card.add-form
    //-         .card-header
    //-             h5 Đăng nhập
    //-         .card-body
    //-             form.login-form(method="POST")
    //-                 div.login-form
    //-                     label Email
    //-                         input.form-control(type="text", name="email", value=values ? values.email : '')
    //-                 div
    //-                     label Mật khẩu
    //-                         input.form-control(type="password", name="password", value=values ? values.password:'')
    //-                 button.btn.btn-info Login
    .main_container
        if messages.error
            //- each error in errors
            .alert.alert-danger= messages.error
        .center
            h1 Đăng nhập
            form(method='POST')
                .text_field
                    input(type='text', required='', name="email", value=values ? values.email : '')
                    span
                    label Tên đăng nhập
                .text_field
                    input(type='password', required='', name="password", value=values ? values.password:'')
                    span
                    label Mật khẩu
                .pass Quên mật khẩu?
                input(type='submit', value='Đăng nhập')
                .sign_up
                    | Chưa có tài khoản 
                    a(href='/register') Tạo mới